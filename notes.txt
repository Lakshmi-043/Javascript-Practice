Javascript provides behaviour to code for creating webpages.
To do any functionalities for a web creation we use javascript.
We can make dynamic pages,link to backend side and to validate those.
Javascript is a scripting language.
Javascript starts from left to right acting.
javascript accept caamelcase format-means second word of starting letter should be capital.
javascript printing statement is console.log

1>......VARIABLE........<1
Variable is like a memory there we can store values
Types of variables-->1.Var-->rules
---variable---- 
var a = 50
console.log(a)
a=55
var a = 60
console.log(a)
---let------
//1.declaration
//let b
//2.initialization
let b = 40
//3.reuse 4.reinitialization
b = 66
//5.redeclaration
//let b .....redeclaration not accept
console.log(b);
---const----
/const - constant
//1.declaration &2. //initialization
const c = 30
//reuse
c = 20 //3.reuse 4.reinitialization also not accept
// 5 .redeclaration
//const c = 50 ...not accept redeclaration
console.log(c);

2>.....PRINTING STEATEMENTS......

----Function:(it is a heart of javascript)----
->block of code(execute n no.of code)
->We can reusable the code
->reduce the repitition of code
->easily maintained-changes will make at one place and runs whole code.
Example: Employee Salary ,ATM machine ,click button
Use:From static to dynamic creation of page we use function.
Syntax: function one(parameter){
    statement
    return
    yield
}
one(argument)-call function--the function calling method called invoke.
Example:function name1(){
    console.log{"naveen"};
}
name1()    //when we call the function it prints the values
Parameter / argument
function para(a){          //parameter acts like variable that stores the values
        console.log(a);
}
para("hello")
Note: The values are going to stored as a hierarchial way.
return()-for reuse the code
Types of Functions:
1.named function
syntax: function named(parameter){
    statement
}
named("named function")         
2.anonymous function
Syntax: let anonymous = function(parameter){
    statement
}
anonymous("anonymous function")
3.arrow function
Syntax: let arrow = (parameter)=>{
        statement
}
arrow("arrow function")
Scope: var-globl scope/function scope
      let,const-block scope/local scope
Hoisting: 
IIFE:Self invoke function
Syntax: (function(products){
       alert("now sales on going ${products} 50% off.");
})("shoes")

Higher Order Function & callback Function
function function1(){
    console.log("higher order function");
}
function function2(){
    console.log("callback function");
}
function1(function2())-one function inside we call another function is called callback function 
and the outside function is called higher order function
Generator Function:
Syntax: function* gen(){
    yield "Cashback offer"
    yield "100% cashback offer"
}


Data Structure

//Object Spread Operator
let obj1 = {
    name1 : "Surya",
    role : "Javadeveloper",
    salary : 600000
}
let totalObj = {......obj1,.....onj2}
console.log(totalObj);

//rest Operator
....Array Higher order function....
We use array higher order function in real time as:
for product searches,for entering records
Types of Array higher order function
1.for each()
2.filter()- is used for product searches
3.map()- for displaying selected fields
4.find()- for searching a specific record
5.reduce()- for bill calculations 
6.some()- for checking if any record matches a condition
7.every()- for validating all records
....String....
A string is a sequence of characters used to store text data.
....String Methods:....
charAt()-returns the character at a specified index
charCodeAt()-is used to get the Unicode value of that character
length-is a property used to find the number of characters in a string
slice-Extract part of string
toUpperCase()-Converts text to uppercase
toLowerCase()-Converts text to lowercase
trim()-Removes extra spaces from the beginning and end
includes()-Checks whether a string contains a specific value
split()-Converts a string into an array
replace()-Replaces a value with another value
startsWith()-Checks whether a string starts with a specific value
endsWith()-Checks whether a string ends with a specific value
indexOf()-Returns the position of a character or word.


